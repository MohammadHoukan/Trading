---
description: Spawn a new Grid Trading Worker for a specific pair
---

1. Start a Worker (Example: SOL/USDT)
   - Change `--pair` and `--grids` as needed.
// turbo
```bash
python workers/grid_bot.py --pair SOL/USDT --grids 20
```
